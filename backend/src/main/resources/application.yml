spring:
  datasource:
    url: jdbc:mysql://mysql:3306/blog  # 'mysql'은 컨테이너 이름
    username: root
    password: thrmEkel123!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
